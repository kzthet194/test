name: remote ssh command
on: [push]
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: executing remote ssh commands using ssh key
          uses: appleboy/ssh-action@v1.0.3
          with:
            host: "ssh.kidstart.sg"
            username: "u1441-4dd9gxp75nrt"
            key: ${{ secrets.SITEGROUND_KEY }}
            port: "18765"
            script: whoami
